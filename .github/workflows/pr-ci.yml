name: 'Build & Test'
on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master
jobs:
  Test1:
    name: "tests on ubunt"
    runs-on: ubuntu-latest
    steps:
      - name: echo
        run: |
          echo "I'm running"
